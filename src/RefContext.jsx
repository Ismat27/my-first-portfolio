// import { createContext, useRef } from "react";
// export const RefsContext = createContext()

// const RefContextProvider = ({props}) => {
//     const homeRef = useRef(null)
//     const latestWorkRef = useRef(null)
//     const contactRef = useRef(null)
//     const aboutRef = useRef(null)

//     return (
//         <RefsContext.Provider value={{homeRef, latestWorkRef, contactRef, aboutRef}}>
//             {props.children}
//         </RefsContext.Provider>
//     )
// }

// export default RefContextProvider
